#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

override_dh_usrlocal:

# The Go compiler emits DWARF in compressed sections, which dwz(1) cannot read.
# It does not skip the file -- it fails, and takes the whole build down with it
# (Debian #931891).
#
# Excluded by PATH, not by name: both Go binaries live under
# /usr/libexec/timeshift and one of them is called "timeshift", so -Xtimeshift
# would also exclude /usr/bin/timeshift, which is the Vala binary and compresses
# fine. The three Vala binaries are still compressed.
override_dh_dwz:
	dh_dwz -Xusr/libexec/timeshift/

%:
	dh $@
